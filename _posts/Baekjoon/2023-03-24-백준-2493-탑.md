---
layout: single
title: "[백준] 2493. 탑"
comments: true
category: Baekjoon
---

## 2493.   탑

---

### Code

```java
package baekjoon;

import java.util.*;
import java.io.*;

public class 탑_2493 {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        int N = Integer.parseInt(br.readLine());
        Stack<Integer> stack = new Stack<>();
        LinkedList<Top> list = new LinkedList<>();

        StringTokenizer st = new StringTokenizer(br.readLine(), " ");
        for(int i=0; i<N; i++){
            int a = Integer.parseInt(st.nextToken());
            list.add(new Top(i+1, a));
        }

        for(int i=N-1; i>=0; i--){
            boolean flag = false;
            for(int j=i-1; j>=0; j--) {
                if (list.get(i).height < list.get(j).height) {
                    stack.push(list.get(j).idx);
                    flag = true;
                    break;
                }
            }
            if(!flag) stack.push(0);
        }

        StringBuilder sb = new StringBuilder();

        while(!stack.isEmpty())
            sb.append(stack.pop()+" ");
        System.out.println(sb.toString());
        br.close();
    }

    static class Top{
        int idx;
        int height;
        public Top(int idx, int height){
            this.idx = idx;
            this.height = height;
        }
    }
}

```
